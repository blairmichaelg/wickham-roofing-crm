# === Web Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.4

# === HTTP Client ===
httpx>=0.27.0

# === Task Queue (async-native, Upstash-friendly) ===
arq>=0.26.0

# === Redis ===
redis>=5.0.0

# === AI Provider ===
google-genai>=1.0.0

# === PDF Generation ===
reportlab>=4.0

# === PDF Parsing (EagleView table extraction) ===
pdfplumber>=0.11.0

# === Security & Authentication ===
PyJWT>=2.8.0
# bcrypt is pinned to a safe range (>=3.2.2,<4.0.0): allows security patch updates within 3.x
# while strictly excluding bcrypt 4.0.0+ / 5.0.0+ which raises ValueError on inputs > 72 bytes
# instead of silently truncating, breaking passlib compatibility and causing 500 auth errors.
bcrypt>=3.2.2,<4.0.0
passlib>=1.7.4

# === Configuration & Validation ===
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# === Structured Logging ===
structlog>=24.0.0

# === Image Processing (V3 Inspection Engine) ===
Pillow>=10.0
